
server:
  port: 8050
spring:
  application:
    name: feign
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    instance-id: fegin-call
#断路器，起到降级作用，避免服务雪崩
feign:
    hystrix:
      enabled: true
